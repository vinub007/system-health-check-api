# Development & CI tooling — pinned for reproducible runs
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

# Linting
ruff==0.8.4

# Type checking
pyright==1.1.391

# Security SAST
bandit==1.8.0

# Test coverage (already in requirements.txt but duplicated here for dev convenience)
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
